<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/watchLaterCardView"
    android:layout_width="match_parent"
    android:layout_height="@dimen/alarm_item_height"
    app:cardCornerRadius="@dimen/item_container_radius"
    app:cardElevation="@dimen/item_container_elevation">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/poster"
            android:layout_width="@dimen/alarm_item_poster_width"
            android:layout_height="match_parent"
            android:contentDescription="@string/poster_description"
            android:scaleType="centerCrop"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:shapeAppearance="@style/PoserLeftCornerCut"
            tools:src="@tools:sample/avatars" />

        <TextView
            android:id="@+id/title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:maxLines="2"
            android:minHeight="@dimen/alarm_item_title_min_height"
            android:padding="@dimen/alarm_item_text_padding"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"
            app:layout_constraintLeft_toRightOf="@id/poster"
            app:layout_constraintRight_toLeftOf="@id/menuButton"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Title" />

        <TextView
            android:id="@+id/dateTime"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:gravity="center"
            android:padding="@dimen/alarm_item_text_padding"
            android:textAppearance="@style/TextAppearance.AppCompat.Medium"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toRightOf="@id/poster"
            app:layout_constraintRight_toLeftOf="@id/menuButton"
            app:layout_constraintTop_toBottomOf="@id/title"
            tools:text="@tools:sample/date/hhmm" />

        <ImageButton
            android:id="@+id/menuButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:background="@null"
            app:tint="?colorSecondary"
            android:clickable="true"
            android:contentDescription="@string/alarm_item_menu_btn_description"
            android:minWidth="@dimen/alarm_item_menu_btn_min_size"
            android:minHeight="@dimen/alarm_item_menu_btn_min_size"
            android:src="@drawable/ic_more_vert_24"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>